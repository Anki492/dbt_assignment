version: 2

sources:
  - name: raw
    database: DBT_DB
    schema: RAW
    tables:
      - name: customers
      - name: orders

models:
  - name: incremental_orders
    columns:
      - name: amount
        tests:
          - not_negative
