models:
  - name: ephemeral_customers
    columns:
      - name: customer_id
        data_tests:
          - unique

  - name: incremental_orders
    columns:
      - name: amount
        data_tests:
          - unique
          - positive
