models:
  - name: ephemeral_customers
    columns:
      - name: customer_id
        tests:
          - unique

  - name: incremental_orders
    columns:
      - name: amount
        tests:
          - unique
          - positive

