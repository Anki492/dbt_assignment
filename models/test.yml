models:
  - name: ephemeral_customers
    columns:
      - name: customer_id
        data_tests:
          - unique
          - not_null
  - name: incremental_orders
    columns:
      - name: amount
        data_tests:
          - not_negative